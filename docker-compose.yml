services:
  jan-db:
    image: postgres:18
    container_name: ${STACK_PREFIX}jan-db
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      start_period: 20s
      interval: 10s
      retries: 5
      timeout: 5s
    volumes:
      -  ${ABS_PROJECT_DIR}/postgresql:/var/lib/postgresql
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${DB_PORT:-5432}:5432"
    networks:
      - ${STACK_PREFIX}jan-internal

  jan-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ${STACK_PREFIX}jan-backend
    restart: unless-stopped
    depends_on:
      jan-db:
        condition: service_healthy
    environment:
      DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${STACK_PREFIX}jan-db:5432/${POSTGRES_DB}"
      SECRET_KEY: ${SECRET_KEY}
      CORS_ORIGINS: ${CORS_ORIGINS}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-30}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID:-}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET:-}
    expose:
      - "8000"
    networks:
      - ${STACK_PREFIX}jan-internal
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${STACK_PREFIX}jan-api.rule=Host(`${APP_HOST}`) && (PathPrefix(`/auth`) || PathPrefix(`/items`) || PathPrefix(`/health`) || PathPrefix(`/docs`) || PathPrefix(`/openapi.json`))"
      - "traefik.http.routers.${STACK_PREFIX}jan-api.entrypoints=websecure"
      - "traefik.http.routers.${STACK_PREFIX}jan-api.tls.certresolver=letsencrypt"
      - "traefik.http.services.${STACK_PREFIX}jan-api.loadbalancer.server.port=8000"

  jan-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ${STACK_PREFIX}jan-frontend
    restart: unless-stopped
    depends_on:
      - jan-backend
    environment:
      BACKEND_URL: ${STACK_PREFIX}jan-backend:8000
    expose:
      - "80"
    networks:
      - ${STACK_PREFIX}jan-internal
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${STACK_PREFIX}jan-app.rule=Host(`${APP_HOST}`)"
      - "traefik.http.routers.${STACK_PREFIX}jan-app.entrypoints=websecure"
      - "traefik.http.routers.${STACK_PREFIX}jan-app.tls.certresolver=letsencrypt"
      - "traefik.http.services.${STACK_PREFIX}jan-app.loadbalancer.server.port=80"


networks:
  ${STACK_PREFIX}jan-internal:
    driver: bridge
  proxy:
    external: true
